@model IEnumerable<BTL_Web_Cinema.Models.Entities.TheLoai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/LayOut_Admin.cshtml";
}

<h2>THỂ LOẠI</h2>

<p>
    @Html.ActionLink("THÊM MỚI", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTheLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTheLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoai }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoai })
        </td>
    </tr>
}

</table>
